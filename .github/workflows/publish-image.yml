name: publish-image
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: read
  packages: write
jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push
        run: |
          docker buildx build --build-context kit=./vendor/kit \
            --label org.opencontainers.image.source=https://github.com/Opus-Perpetuus/imperium-subject-ventas \
            --annotation org.opencontainers.image.source=https://github.com/Opus-Perpetuus/imperium-subject-ventas \
            --annotation index:org.opencontainers.image.source=https://github.com/Opus-Perpetuus/imperium-subject-ventas \
            --annotation manifest:org.opencontainers.image.source=https://github.com/Opus-Perpetuus/imperium-subject-ventas \
            -t ghcr.io/opus-perpetuus/subject-ventas:0.1.0 \
            -t ghcr.io/opus-perpetuus/subject-ventas:latest \
            --push \
            .
          docker push ghcr.io/opus-perpetuus/subject-ventas:0.1.0
          docker push ghcr.io/opus-perpetuus/subject-ventas:latest
